알고리즘이란?
- 문제를 푸는 계산 혹은 계산 과정을 정의하는 방법
- 컴퓨터 프로그램에 대응하는 수학적 표현

계산 모델?
- 알고리즘이 할 수 있는 연산과 그 연산에 걸리는 비용(시간)을 결정

(1) 어셈블리어에서의 계산 모델
- 임의 접근 기계
* 특징으로는 동적 메모리를 할당하지 않는다는 점
* RAM의 동작과 비슷하게 array 방식으로, 계산이 이루어진다.
* array 공간을 차지하게 되는 만큼, 알고리즘의 공간이라고 생각하면 된다.

(2) 그 이후로 나온 계산 모델
- 포인터 머신
* 특징으로는 동적 메모리 할당이 가능하다.
* Linked list의 근간인 Pointer 방식을 사용한다.

(3) In python
- 매우 많은 계산 모델이 존재하며, array 그리고 pointer를 모두 사용한다.
* (1)에서와 똑같이 array를 사용하지만 list라고 불리며 이는 Linked List와는 별개다.
* 소요되는 비용도 다르다. Linked List는 L[i] = L[j] + 5를 계산한다고 하면 선형 시간이 소요되지만
* Python의 List는 상수 시간이 소요된다.

- OOP 스타일로 구현하여 pointer 사용이 가능하다.
* 참조할 때도 상수 시간이 소요된다.
